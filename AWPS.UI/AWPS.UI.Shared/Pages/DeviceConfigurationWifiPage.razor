@page "/DeviceConfiguration/Wifi"

@if(base.RendererInfo.Name is not Renderer.WebView)
{
    <DeviceConfigurationWifiPageContent @rendermode=@RenderMode.InteractiveWebAssembly />
}
else
{
    <DeviceConfigurationWifiPageContent />
}
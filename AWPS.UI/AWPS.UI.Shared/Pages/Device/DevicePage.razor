@page "/Device/{Id}"
@attribute [Authorize]
@code
{
    [Parameter] public required string Id { get; set; }
}

@if(base.RendererInfo.Name is not Renderer.WebView)
{
    <DevicePageContent Id=@Id @rendermode=@RenderMode.InteractiveWebAssembly />
}
else
{
    <DevicePageContent Id=@Id />
}